{
  "name": "backend-boilerplate-fastapi",
  "version": "1.0.0",
  "scripts": {
    "build": "cd app && pip3 install -r requirements.txt",
    "start": "cd app && sanic main.app",
    "test": "cypress run"
  },
  "devDependencies": {
    "cypress": "^9.7.0"
  }
}
